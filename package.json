{
  "devDependencies": {
    
    "vue": "^3.4.21"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "build": "vitepress build docs",
    "elog:init": "elog init",
    "sync:local": "node ./delcache.js && elog sync -e .elog.env",
    "sync": "elog sync",
    "clean": "elog clean"
  },
  "dependencies": {
    "@elog/cli": "^0.13.3",
    "vitepress": "^1.0.2"
  }
}
